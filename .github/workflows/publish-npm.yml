name: Publish npm

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Versión a publicar (debe coincidir con package.json)"
        required: true
        default: "0.1.5"

permissions:
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          path: swagger-viewer

      - uses: actions/checkout@v4
        with:
          repository: Jeff-Aporta/neon-glass
          path: neon-glass

      - uses: actions/checkout@v4
        with:
          repository: Jeff-Aporta/front-shared
          path: front-shared

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          registry-url: "https://registry.npmjs.org"
          scope: "@jeff-aporta"

      - name: Verificar versión
        working-directory: swagger-viewer
        run: |
          PKG=$(node -p "require('./package.json').version")
          if [ "$PKG" != "${{ github.event.inputs.tag }}" ]; then
            echo "package.json ($PKG) ≠ tag solicitado (${{ github.event.inputs.tag }})"
            exit 1
          fi

      - run: npm ci
        working-directory: swagger-viewer
      - run: npm run build
        working-directory: swagger-viewer
      - run: npm publish --access restricted
        working-directory: swagger-viewer
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
